# The flash, divided.
#
# The default single-app table has no room for a filesystem, and a
# filesystem is what keeps bin/ and lib/ out of the app image: a program
# uploaded to luafs runs without a reflash, which is what the UEFI
# system partition gives the other platforms.
#
# Offsets are written out rather than left blank. A blank offset packs
# each partition against the one before it, so adding a partition moves
# every partition after it -- and a moved data partition is a partition
# whose contents are now garbage. Fixed offsets mean an edit here breaks
# the build rather than the filesystem.
#
# An app partition must start on a 64KB boundary, a data partition on a
# 4KB one. 0x750000 in total, which fits the Cardputer's 8MB as well as
# the T-Deck's 16MB, so one table serves every target.
#
# luafs is 4MB because it carries lib/ and task/ as well as bin/: the
# image holds what is needed to mount this volume and the tree above it
# is read from here. The whole of lib/, task/, bin/ and etc/ is about
# 1.2MB of source, and a 4096-byte sector rounds each file up.
#
# config is last so that luafs keeps its offset: a partition that moves
# is a partition whose contents are garbage, and this one is being added
# to a board that already has a filesystem at 0x310000.
#
# It is separate from luafs because it outlives it. Everything in luafs
# comes from the build -- tools/mkfatimg.lua writes bin/, lib/, task/
# and etc/ from the tree -- so reflashing the filesystem replaces the
# whole volume, and anything the machine learned about itself goes with
# it. /etc/wifi.lua lived there and was lost exactly that way.
#
# 256KB, which is 64 sectors of the 4096-byte erase block. Small on
# purpose: what belongs here is what a machine knows about itself and
# cannot be told again by a build.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x6000,
phy_init,   data, phy,      0xf000,   0x1000,
factory,    app,  factory,  0x10000,  0x300000,
luafs,      data, fat,      0x310000, 0x400000,
config,     data, fat,      0x710000, 0x40000,
